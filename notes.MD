# Example graph

## connect MQTTX > getting started
1. show how to connect to the public broker -> how this affects the graph
2. show how to sent a message -> on the graph
3. show how to subscribe to a topic -> on the graph

## Students connect via MQTTX > explore
1. send messages to eachother -> make secret channels -> have people find channels

# Example 1 & 7 (Button) > connect hardware to website to Figma
1. show example 1 and send messages to it
2. ask students to send messages to the topic (sensor/value)
3. connect Arduino and click button
4. show Figma and what the button can do
5. show Example 7 and control Figma -> someone else control my Figma

# Example 2 & 3 (LDR + potentiometer) > Sensors become Values > values you can design with
1. First show example 1 and get the values from the LDR
2. swap to use the Potentiometer > different sensor, same interaction
3. show Figma > same data - different output
3. show example 3 > same sensor different interaction
4. Also show Figma simontaniously (they are just values)

# Example 4
1. Use already existing hardware to get the interaction you need

# Example 5 > we dont care who listens, this scales
1. Showcase the website
2. Show it up in the Figma prototype
3. turn on the MQTT devices and show it as well
4. Show figma make to have a built app

# Example 6 > we can make quite some advanced things
1. Show the canvas
2. Have people connect to it and collaboratively draw someting (MDD logo)
3. open touch designer and load sketch

# Final example, show mobile app and control godot.